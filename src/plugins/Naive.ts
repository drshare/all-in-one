import {
    // create naive ui
    create,
    // component
    NButton,
    NCard,
    NBackTop,
} from 'naive-ui';

const naive = create({
    components: [NButton, NCard, NBackTop],
});

export default naive;
